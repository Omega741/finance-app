# Core harness
pandas>=2.0
numpy>=1.24

# Real market data
yfinance>=0.2.40

# Paper trading broker
alpaca-py>=0.26.0

# LLM agents
requests>=2.31.0       # local Ollama backend (default)
anthropic>=0.40.0      # optional — only if LLM_BACKEND=anthropic

# Finance sentiment (FinBERT). CPU torch is plenty for this 110M model:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2
transformers>=4.40

# Journal persistence
duckdb>=0.10.0

# Config
python-dotenv>=1.0.0

# Optional: FRED macro data
# fredapi>=0.5.0
